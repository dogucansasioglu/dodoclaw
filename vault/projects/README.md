# Projects

Store project notes, decisions, and context here.

Each file = one project. Use wiki-links (`[[Project Name]]`) to connect related projects.

Example structure:
```markdown
# My App

- **Status:** Active
- **Stack:** Next.js, Convex
- **Key decisions:**
  - Using server actions over API routes
  - SQLite for local dev, Postgres for prod
```
